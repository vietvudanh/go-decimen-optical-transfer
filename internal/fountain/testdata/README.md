Generated by running the *original* TypeScript functions from
`decimen-optical-transfer/shared/fountain.ts` under Node — spliced out of the
source verbatim, not re-typed — so `fountain_test.go` holds this Go port to the
JavaScript sender's bytes rather than only to itself.

- `composition.txt` — `k sessionId seq  idx,idx,…` from `frameComposition`
- `soliton.txt` — `dlog x value`, `cdf k first mid last`, `idx k seq indices`
